# ==============================================================
#  config.py  —  全局常量配置中心
# ==============================================================

# 窗口与画布
WIDTH  = 800
HEIGHT = 800

# 曲线采样精度
NUM_SEGMENTS = 1000          # 曲线离散采样点数

# 控制点上限（对象池大小）
MAX_CONTROL_POINTS = 100

# 反走样参数
AA_RADIUS = 1.5              # 距离衰减半径（单位：像素），覆盖 3×3 邻域
AA_KERNEL_HALF = 1           # 邻域半径（±1 → 3×3 grid）

# 颜色
COLOR_CURVE_BEZIER  = (0.0, 1.0, 0.0)   # 贝塞尔曲线：绿色
COLOR_CURVE_BSPLINE = (0.2, 0.6, 1.0)   # B 样条曲线：蓝色
COLOR_CONTROL_POINT = (1.0, 0.0, 0.0)   # 控制点：红色
COLOR_CONTROL_LINE  = (0.5, 0.5, 0.5)   # 控制折线：灰色
COLOR_CURVE_AA_OFF = (0.0, 1.0, 0.0)   # 反走样关：绿色
COLOR_CURVE_AA_ON  = (1.0, 1.0, 0.0)   # 反走样开：黄色